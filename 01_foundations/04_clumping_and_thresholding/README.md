# 04 — Manual clumping + thresholding (C+T)

**Question:** How do LD pruning and GWAS P-value thresholds jointly determine which SNPs enter a PRS?

Two thresholds were scored manually so the effect of changing the SNP set is explicit before moving to PRSice.

The score and weight files here are the actual outputs from that exercise.
